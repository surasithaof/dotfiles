# Commit Messages

- Conventional Commits format: `type(scope): subject` — `feat`, `fix`, `test`, `chore`,
  `refactor`, etc. Add `!` after the type (or a `BREAKING CHANGE:` footer) for a breaking change.
- Scope is optional but preferred when a repo has clear areas.
